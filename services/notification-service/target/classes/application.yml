server:
  port: 8086

spring:
  application:
    name: notification-service

  datasource:
    url: jdbc:mysql://localhost:3306/notification_db
    username: root
    password: Axelblaze10

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: SECRET_KEY_SECRET_KEY_SECRET_KEY_256_BIT_SECRET_KEY

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/